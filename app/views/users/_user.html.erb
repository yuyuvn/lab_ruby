<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= button_to user_path(user), method: :delete, 
								data: { confirm: "You sure?" }, class: 'btn-link' do %>
		Delete
	<% end %>
								  
  <% end %>
</li>